// Synthetic entry for design-sync. This repo is a Next.js app, not a
// publishable component package, and this sync is scoped to tokens/fonts
// only (see .design-sync/NOTES.md) — no components are exported here.
export {};

"""Shared foundations: configuration, database connection, ORM models, exceptions.

An `__init__.py` file - even an empty one - is what makes a folder importable as
a Python *package*. Without it, `from core.config import settings` would fail.
"""
